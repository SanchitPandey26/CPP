#include<bits/stdc++.h>
using namespace std;

int trap(vector<int> height){
    
}

int main(){
    int n;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; i++){
        cin >> arr[i];
    }
    cout << trap(arr);
}