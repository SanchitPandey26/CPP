#include <bits/stdc++.h>
using namespace std;

int main(){
    // Everything is the same as set, except it doesn't store elements in order.
    // All the functions are same except lower_bound and upper_bound, it doesn't work.
    // Generraly for all the functions the time complexity is O(1), but in some worst case
    // the time complexity is O(n). Worst case is very rare.


   
}