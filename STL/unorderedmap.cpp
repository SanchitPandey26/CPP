#include <bits/stdc++.h>
using namespace std;

int main(){
    // Everything is the same as map, except we store keys in random order, i.e, the map is not sorted.
    // It works in O(1) time complexity, just in worst case it goes O(n).
   
}